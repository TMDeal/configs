# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Variables
#
# mod variables
set $mod Mod4
set $alt Mod1

# font variables
set $font pango:Anonymous Pro 12

# color variables
# set_from_resource $background   background #121212
# set_from_resource $foreground   foreground #d7d7d7
# set_from_resource $dark_black   color0     #393939
# set_from_resource $black        color8     #535551
# set_from_resource $dark_red     color1     #ca674a
# set_from_resource $red          color9     #ea2828
# set_from_resource $green        color2     #96a967
# set_from_resource $dark_green   color10    #87dd32
# set_from_resource $dark_yellow  color3     #d3a94a
# set_from_resource $yellow       color11    #f7e44d
# set_from_resource $dark_blue    color4     #5778c1
# set_from_resource $blue         color12    #6f9bca
# set_from_resource $dark_magenta color5     #9c35ac
# set_from_resource $magenta      color13    #a97ca4
# set_from_resource $dark_cyan    color6     #6eb5f3
# set_from_resource $cyan         color14    #32dddd
# set_from_resource $dark_white   color7     #a9a9a9
# set_from_resource $white        color15    #e9e9e7

set $background   #121212
set $foreground   #d7d7d7
set $dark_black   #393939
set $black        #535551
set $dark_red     #ca674a
set $red          #ea2828
set $green        #96a967
set $dark_green   #87dd32
set $dark_yellow  #d3a94a
set $yellow       #f7e44d
set $dark_blue    #5778c1
set $blue         #6f9bca
set $dark_magenta #9c35ac
set $magenta      #a97ca4
set $dark_cyan    #6eb5f3
set $cyan         #32dddd
set $dark_white   #a9a9a9
set $white        #e9e9e7

set $FONT pango:Anonymous Pro 12

# color variablse
set $CLIENT_BACKGROUND #3E3E3E
set $CLIENT_TEXT       #FFFFFF
set $CLIENT_INACTIVE   #4B6849
set $CLIENT_FOCUS      #888888

set $BAR_BACKGROUND #2D2E30 
set $BAR_TEXT       #FFFFFF
set $BAR_SEPARATOR  #888888
set $BAR_INACTIVE   #999999

set $URGENT #E16B79

# direction variables
set $left  h
set $down  j
set $up    k
set $right l

# workspace variables
set $ws1  1:I
set $ws2  2:II
set $ws3  3:III
set $ws4  4:IV
set $ws5  5:V
set $ws6  6:VI
set $ws7  7:VII
set $ws8  8:VIII
set $ws9  9:IX
set $ws10 10:X

# fonts
font $font

# background
set $wallpaper_picture $HOME/.config/i3/backgrounds/jpg/relicanth.jpg
set $WALLPAPER_PICTURE $HOME/.config/i3/backgrounds/jpg/ninjask.jpg

# autostart
exec --no-startup-id compton --config $HOME/.config/compton.conf
exec --no-startup-id feh --bg-scale $wallpaper_picture
exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec --no-startup-id nm-applet

# class                 border     background   text         indicator    child_border
client.focused          $black     $black       $dark_white  $dark_black  $dark_black
client.focused_inactive $black     $dark_black  $white       $dark_black  $dark_black
client.unfocused        $black     $black       $white       $dark_black  $dark_black
client.urgent           $dark_red  $dark_red    $black       $dark_red    $dark_red
client.background       $background

# window borders
new_window pixel 3
new_float  pixel 3

floating_modifier $mod

# dont change focus on mouse hover
focus_follows_mouse no

# move back and forth between workspaces easier
workspace_auto_back_and_forth yes

# start a terminal
bindsym $mod+Return exec --no-startup-id termite

# prompt to take screenshot
bindsym $mod+p exec --no-startup-id scrot ~/Pictures/screenshots/screenshot_%Y_%m_%d_%H_%M_%S.png && notify-send "SREENSHOT TAKEN"

# kill focused window
bindsym $mod+Shift+q kill

# rofi
bindsym $mod+d exec --no-startup-id j4-dmenu-desktop --term="termite" --no-generic --use-xdg-de --dmenu="rofi -dmenu -i -p 'run:'"

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -q -D pulse sset Master 5%+ && pkill -RTMIN+1 i3blocks 
bindsym XF86AudioLowerVolume exec --no-startup-id amixer -q -D pulse sset Master 5%- && pkill -RTMIN+1 i3blocks
bindsym XF86AudioMute exec --no-startup-id amixer -q -D pulse sset Master toggle && pkill -RTMIN+1 i3blocks

# Sreen brightness controls
bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 20
bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 20

no_focus [window_role="pop-up"]

for_window [window_role="pop-up"] floating enable
for_window [window_type="dialog"] floating enable
for_window [window_type="menu"] floating enable

assign [instance="audacious"] $ws9
assign [class="Zeal"] $ws8
assign [instance="htop"] $ws8
assign [instance="libreoffice"] $ws10

# change focus
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

# move focused window
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

# split in horizontal orientation
bindsym $mod+minus split v

# split in vertical orientation
bindsym $mod+backslash split h

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+comma focus parent

# focus the child container
bindsym $mod+period focus child

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10
bindsym $mod+equal scratchpad show

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10
bindsym $mod+Shift+equal move scratchpad

bindsym $mod+m bar hidden_state toggle

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec --no-startup-id "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
bindsym XF86ScreenSaver exec --no-startup-id $HOME/.config/i3/scripts/i3lock-wrapper -l
bindsym $mod+Escape exec --no-startup-id $HOME/.config/i3/scripts/i3exit

mode "resize" {
    # resize with directional keys
    bindsym $left             resize grow     left 10 px or 10 ppt
    bindsym Shift+$left       resize shrink   left 10 px or 10 ppt

    bindsym $down             resize grow     down 10 px or 10 ppt
    bindsym Shift+$down       resize shrink   down 10 px or 10 ppt

    bindsym $up               resize grow     up 10 px or 10 ppt
    bindsym Shift+$up         resize shrink   up 10 px or 10 ppt

    bindsym $right            resize grow     right 10 px or 10 ppt
    bindsym Shift+$right      resize shrink   right 10 px or 10 ppt

    # same bindings, but for the arrow keys
    bindsym Left              resize grow     left 10 px or 10 ppt
    bindsym Shift+Left        resize shrink   left 10 px or 10 ppt

    bindsym Down              resize grow     down 10 px or 10 ppt
    bindsym Shift+Down        resize shrink   down 10 px or 10 ppt

    bindsym Up                resize grow     up 10 px or 10 ppt
    bindsym Shift+Up          resize shrink   up 10 px or 10 ppt

    bindsym Right             resize grow     right 10 px or 10 ppt
    bindsym Shift+Right       resize shrink   right 10 px or 10 ppt

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

bar {
    status_command i3blocks -c $HOME/.config/i3/i3blocks.conf
    mode dock
    hidden_state hide
    modifier none
    strip_workspace_numbers yes
    workspace_buttons yes
    position top
    font $FONT
    tray_output LVDS1
    tray_padding 0

    colors {
        # set $background #24282B

        #          color
        background $background
        statusline $white
        separator  $black

        #                  border      background  text
        focused_workspace  $background $background $white
        active_workspace   $background $background $white
        inactive_workspace $background $background $black
        urgent_workspace   $background $background $dark_red
        binding_mode       $background $background $dark_red
    }
}
